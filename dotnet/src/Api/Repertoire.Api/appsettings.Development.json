{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Auth": {
    "//": "Local convenience only. Anywhere else these MUST come from Auth__Username / Auth__Password (or user-secrets) — the app refuses to boot outside Development without them.",
    "Username": "erkan",
    "Password": "chess"
  }
}
