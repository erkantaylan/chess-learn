# Views

Each file registers one alternative rendering of the move tree via `RT.registerView(...)`.
They are loaded by `index.html` and must not modify it. See the RT API in index.html
(section "view API").
